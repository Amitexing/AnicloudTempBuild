package pl.flezy.tempbuild.listener;

import org.bukkit.event.Listener;

/**
 * Intentionally empty: DECORATED_POT handling is vanilla.
 * Kept to avoid stale local-file build breakages when updating from older branches.
 */
public class DecoratedPotListener implements Listener {
}
